import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    primary: '#f1f7ed',
    secondary: '#243e36',
    accent: '#7ca982',
    light: '#e0eec6',
    dark: '#c2a83e',
  },
});

export default theme;
